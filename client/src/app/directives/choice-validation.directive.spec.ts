import { ChoiceValidationDirective } from './choice-validation.directive';

describe('ChoiceValidationDirective', () => {
    it('should create an instance', () => {
        const directive = new ChoiceValidationDirective();
        expect(directive).toBeTruthy();
    });
});
