import { QuestionExistsDirective } from './question-exists.directive';

describe('QuestionExistsDirective', () => {
    it('should create an instance', () => {
        const directive = new QuestionExistsDirective();
        expect(directive).toBeTruthy();
    });
});
