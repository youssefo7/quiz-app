import { QuestionFormValidatorDirective } from './question-form-validator.directive';

describe('QuestionFormValidatorDirective', () => {
    it('should create an instance', () => {
        const directive = new QuestionFormValidatorDirective();
        expect(directive).toBeTruthy();
    });
});
